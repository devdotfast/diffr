[package]
name = "fixtures-plugin"
description = "An example diffr plugin: tag fixtures and hide them."
version = "0.1.0"
edition = "2021"
rust-version = "1.85.0"
license = "MIT"
publish = false

[dependencies]
diffr-plugin-sdk = { path = "../../../crates/diffr-plugin-sdk" }
serde = { version = "1.0", features = ["derive"] }
